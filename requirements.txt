# Runtime dependencies for the main kds-occlusion environment.
#
# Install the CUDA-specific PyTorch/Torchvision wheels first, then install this
# file and the external ByteTrack/BoxMOT checkouts as described in README.md.
# Co-DETR and SAM3 intentionally live in separate environments because their
# Python, PyTorch, and NumPy requirements are incompatible with this one.

# Core data, image, and configuration stack
numpy>=2.2,<3.0
Pillow>=10.0,<12.0
PyYAML>=6.0,<7.0
opencv-python>=4.10,<5.0
scipy>=1.10,<2.0

# Detection, tracking, and metric stack
pycocotools>=2.0.8,<3.0
tqdm>=4.66,<5.0
loguru>=0.7,<1.0
tabulate>=0.9,<1.0
filterpy>=1.4.5,<2.0
lapx>=0.5.5,<1.0
Cython>=3.0,<4.0
cython-bbox>=0.1.5
scikit-image>=0.22,<1.0
tensorboard>=2.16,<3.0
thop>=0.1.1
ninja>=1.11
h5py>=3.10,<4.0

# Checkpoint download and H.264-capable comparison videos
huggingface-hub>=0.24,<2.0
imageio>=2.34,<3.0
imageio-ffmpeg>=0.5,<1.0

# Separate environments:
#   Co-DETR: environment.codetr.yml + docs/CODETR_WORKFLOW.md
#   SAM3:    environment.sam3.yml  + docs/SAM3_WORKFLOW.md
