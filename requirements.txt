# Project-side dependencies. Install the CUDA-specific PyTorch/Torchvision
# wheels first, then install this file inside the kds-occlusion environment.
# Recommended environment:
#   conda env create -f environment.yml
#   conda activate kds-occlusion
numpy>=2.2,<3.0
Pillow>=10.0,<12.0
PyYAML>=6.0,<7.0
opencv-python>=4.10,<5.0
pycocotools>=2.0.8,<3.0
tqdm>=4.66,<5.0
loguru>=0.7,<1.0
tabulate>=0.9,<1.0
filterpy>=1.4.5,<2.0
lapx>=0.5.5,<1.0
cython-bbox>=0.1.5
scikit-image>=0.22,<1.0
tensorboard>=2.16,<3.0
thop>=0.1.1
ninja>=1.11
h5py>=3.10,<4.0
# SAM3 is intentionally installed in the separate kds-sam3 environment.
# See environment.sam3.yml and docs/SAM3_WORKFLOW.md.
