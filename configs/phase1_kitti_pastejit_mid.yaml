# Phase-1 treatment arm with moderate per-frame jitter on the pasted occluder
# crops. See phase1_kitti_pastejit_low.yaml — same arm, stronger ranges.
base: phase1_kitti.yaml

tracklet_synthesis:
  output_dir: ../artifacts/phase1/tracklet_synthetic_pastejit_mid
  paste_jitter:
    preset: mid

dataset:
  synthetic_output_dir: ../artifacts/phase1/tracklet_synthetic_pastejit_mid
  output_dir: ../artifacts/phase1/yolox_data_pastejit_mid
